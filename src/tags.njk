---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - post
    - posts
    - tagList
    - todayLearnedTagList
  addAllPagesToCollections: true
layout: layouts/base.njk
eleventyComputed:
  title: Tagged “{{ tag }}”
permalink: /tags/{{ tag | slugify }}/
---
<h1 id="Skip">Tagged “{{ tag }}”</h1>

{% set postslist = collections[tag] %}
{% include "partials/postslist.njk" %}

<p>See <a href="{{ '/tags/' | url }}">all tags</a>.</p>