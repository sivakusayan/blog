---
layout: layouts/base.njk
templateClass: tmpl-post
---

<div>
  <div class="post-content">
    <div class="post-header">
      <h1 id="Skip">{{ title }}</h1>
      <time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time>
      <div class="tags">
        Tagged:
        {%- for tag in tags | filterTagList %}
	      {%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
	      <a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a>{%- if not loop.last %}, {% endif %}
	      {%- endfor %}
      </div>
    </div>
    {{ content | safe | toc | details}}
  </div>
  <a class="back-to-top" href="#Skip">Go back to top &ShortUpArrow;</a>
</div>

<div>
  {%- if collections.posts %}
    {%- set nextPost = collections.posts | getNextCollectionItem(page) %}
    {%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}
    {%- if nextPost or previousPost %}
      <hr>
      <ul class="post-chain">
        {%- if previousPost %}
          <li><span>Previous: </span><a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a>
          </li>
        {% endif %}
        {%- if nextPost %}
          <li><span>Next: </span><a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a>
          </li>
        {% endif %}
      </ul>
    {%- endif %}
  {%- endif %}
</div>