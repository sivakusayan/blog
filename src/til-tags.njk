---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - post
    - posts
    - tagList
    - todayLearnedTagList
  addAllPagesToCollections: true
layout: layouts/base.njk
eleventyComputed:
  title: TIL posts tagged “{{ tag }}”
permalink: /til/tags/{{ tag | slugify }}/
---
<h1 id="Skip">TIL posts tagged “{{ tag }}”</h1>

{% set postslist = collections[tag] | getTodayLearnedPosts %}
{% include "partials/postslist.njk" %}

<p>See <a href="{{ '/til/tags/' | url }}">all TIL post tags</a>.</p>