---
layout: layouts/base.njk
eleventyNavigation:
  key: Home
  order: 1
---
{% from "partials/gerritLink.njk" import gerritLink %}

<div>
  <h1 id="Skip">
    Sayan Sivakumaran
  </h1>
  <p>I'm a software developer with 3 years of experience doing full stack development in React, TypeScript, and C#.</p>
  <p>I have recently started contributing to the <a href="https://www.chromium.org/chromium-projects/">Chromium project</a>, the code that powers Google Chrome and Microsoft Edge. See my work below, or check out <a href="/posts">my blog</a>, if you'd like. 🙂</p>
</div>

<div>
  <h2 id="chromium-contributions">Chromium contributions</h2>
  {% if chromiumChanges.inProgress | length %}
    <h3>In Progress</h3>
    <ul>
      {% for change in chromiumChanges.inProgress %}
        <li>{{ gerritLink(change) }}</li>
      {% endfor %}
    </ul>
    <!-- We only want to show separate headings if there is some change in progress. 
         Otherwise the headings add needless noise.-->
    <h3>Merged</h3>
  {% endif %}
  <ul>
    {% for change in chromiumChanges.merged %}
      <li>{{ gerritLink(change) }}</li>
    {% endfor %}
  </ul>
</div>